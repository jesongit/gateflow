# Plan: update hello.txt

1. In the target workspace, apply the requested `hello.txt` mutation. For an
   `append-line` mutation, preserve the existing content and append the
   requested line.
2. Review the resulting diff, commit it, and push the commit to the target
   repository.
3. Report the pushed change and validation result through the formal
   Workspace Protocol output.
